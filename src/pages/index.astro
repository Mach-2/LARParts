---
import artists from "../data/directory.json";

function slugify(value: string) {
	return value
		.toLowerCase()
		.trim()
		.replace(/[^a-z0-9]+/g, "-")
		.replace(/^-|-$/g, "");
}

const kingdomMap = new Map<string, number>();

for (const artist of artists) {
	const kingdom = artist.Kingdom?.trim();

	if (!kingdom) {
		continue;
	}

	kingdomMap.set(kingdom, (kingdomMap.get(kingdom) ?? 0) + 1);
}

const kingdoms = [...kingdomMap.entries()]
	.map(([name, artistCount]) => ({
		label: "Kingdom",
		name,
		artistCount,
		href: `/kingdoms/${slugify(name)}/`,
		image: name === "Northern Lights" ? "/photos/NL_Heraldry1.png" : "",
	}))
	.sort((a, b) => a.name.localeCompare(b.name));

const artistCount = artists.length;
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Northern Lights Artisan Directory</title>
	</head>
	<body>
		<main class="page">
			<section class="hero">
				<p class="eyebrow">Amtgard Artisan Directory</p>
				<h1>Artisan Directory</h1>
				<p class="intro">
					Browse all artists in the directory, or select a kingdom to view local artists.
				</p>
			</section>

			<a class="featured-card" href="/artists/">
				<div class="featured-mark" aria-hidden="true">A</div>
				<div class="featured-content">
					<p class="card-label">Full Directory</p>
					<h2>All Artists</h2>
					<p>{artistCount} {artistCount === 1 ? "artist" : "artists"}</p>
				</div>
			</a>

			<section class="kingdom-grid" aria-label="Kingdoms">
				{
					kingdoms.map((kingdom) => (
						<a class="kingdom-card" href={kingdom.href}>
							<div class="kingdom-mark">
								{kingdom.image ? (
									<img src={kingdom.image} alt="" loading="lazy" />
								) : (
									<span>{kingdom.name.charAt(0)}</span>
								)}
							</div>

							<div class="kingdom-content">
								<p class="card-label">Kingdom</p>
								<h2>{kingdom.name}</h2>
								<p>
									{kingdom.artistCount}{" "}
									{kingdom.artistCount === 1 ? "artist" : "artists"}
								</p>
							</div>
						</a>
					))
				}
			</section>
		</main>
	</body>
</html>

<style>
	:global(body) {
		margin: 0;
		font-family: Georgia, serif;
		background:
			radial-gradient(circle at top left, rgba(201, 154, 46, 0.28), transparent 36%),
			linear-gradient(180deg, #fbf7ed 0%, #e8ead8 100%);
		color: #1c241d;
	}

	.page {
		max-width: 1100px;
		margin: 0 auto;
		padding: 3rem 1.25rem 4rem;
	}

	.hero {
		margin-bottom: 2rem;
	}

	.eyebrow {
		margin: 0 0 0.5rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		font-size: 0.85rem;
		color: #8a671a;
	}

	h1 {
		margin: 0;
		font-size: clamp(2.25rem, 4vw, 4rem);
	}

	.intro {
		max-width: 45rem;
		font-size: 1.05rem;
		line-height: 1.6;
		color: #5e6758;
	}

	.kingdom-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 1.5rem;
		max-width: 1200px;
	}

	.featured-card {
		display: grid;
		grid-template-columns: 112px minmax(0, 1fr);
		align-items: center;
		min-height: 164px;
		margin-bottom: 1.5rem;
		padding: 1.4rem;
		color: inherit;
		text-decoration: none;
		background: rgba(255, 253, 246, 0.96);
		border: 2px solid #1f4d3a;
		border-radius: 8px;
		box-shadow: 0 14px 34px rgba(30, 43, 32, 0.14);
		transition:
			transform 160ms ease,
			box-shadow 160ms ease,
			border-color 160ms ease;
	}

	.featured-card:hover,
	.featured-card:focus-visible,
	.kingdom-card:hover,
	.kingdom-card:focus-visible {
		transform: translateY(-3px);
		border-color: #c99a2e;
		box-shadow: 0 16px 36px rgba(30, 43, 32, 0.16);
	}

	.featured-card:focus-visible,
	.kingdom-card:focus-visible {
		outline: 3px solid #c99a2e;
		outline-offset: 4px;
	}

	.featured-mark {
		width: 88px;
		height: 104px;
		display: grid;
		place-items: center;
		font-size: 3rem;
		font-weight: 700;
		color: #1f4d3a;
		background: #e6dcc0;
		border: 1px solid rgba(31, 77, 58, 0.22);
		border-radius: 8px;
	}

	.featured-content {
		min-width: 0;
	}

	.featured-content h2 {
		margin: 0 0 0.45rem;
		font-size: clamp(1.8rem, 3vw, 2.5rem);
		line-height: 1.1;
	}

	.featured-content p:last-child {
		margin: 0;
		color: #5e6758;
		font-size: 1.05rem;
	}

	.kingdom-card {
		display: grid;
		grid-template-columns: 88px minmax(0, 1fr);
		align-items: center;
		min-height: 148px;
		padding: 1.25rem;
		color: inherit;
		text-decoration: none;
		background: rgba(255, 253, 246, 0.9);
		border: 1px solid #d3c39a;
		border-radius: 8px;
		box-shadow: 0 10px 30px rgba(30, 43, 32, 0.12);
		transition:
			transform 160ms ease,
			box-shadow 160ms ease,
			border-color 160ms ease;
	}

	.kingdom-mark {
		width: 72px;
		height: 88px;
		display: grid;
		place-items: center;
	}

	.kingdom-mark img {
		width: 100%;
		height: 100%;
		object-fit: contain;
	}

	.kingdom-mark span {
		font-size: 2.25rem;
		font-weight: 700;
		color: #1f4d3a;
	}

	.kingdom-content {
		min-width: 0;
	}

	.card-label {
		margin: 0 0 0.4rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		font-size: 0.75rem;
		color: #8a671a;
	}

	.kingdom-content h2 {
		margin: 0 0 0.45rem;
		font-size: 1.45rem;
		line-height: 1.15;
	}

	.kingdom-content p:last-child {
		margin: 0;
		color: #5e6758;
		font-size: 0.98rem;
	}

	@media (max-width: 560px) {
		.featured-card {
			grid-template-columns: 64px minmax(0, 1fr);
			padding: 1rem;
		}

		.featured-mark {
			width: 52px;
			height: 64px;
			font-size: 2.25rem;
		}

		.kingdom-card {
			grid-template-columns: 64px minmax(0, 1fr);
			padding: 1rem;
		}

		.kingdom-mark {
			width: 52px;
			height: 64px;
		}
	}
</style>
